from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass Player(Base):\n    __tablename__ = 'players'\n    \n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    teams = relationship('PlayerTeam', back_populates='player')\n\nclass Rider(Base):\n    __tablename__ = 'riders'\n    \n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n\nclass PlayerTeam(Base):\n    __tablename__ = 'player_teams'\n    \n    id = Column(Integer, primary_key=True)\n    player_id = Column(Integer, ForeignKey('players.id'))\n    rider_id = Column(Integer, ForeignKey('riders.id'))\n    \n    player = relationship('Player', back_populates='teams')\n    rider = relationship('Rider')\n\nclass StageResult(Base):\n    __tablename__ = 'stage_results'\n    \n    id = Column(Integer, primary_key=True)\n    rider_id = Column(Integer, ForeignKey('riders.id'))\n    stage_number = Column(Integer, nullable=False)\n    result_time = Column(String)  # Could be adjusted to a more suitable type\n\nclass Transfer(Base):\n    __tablename__ = 'transfers'\n    \n    id = Column(Integer, primary_key=True)\n    player_id = Column(Integer, ForeignKey('players.id'))\n    rider_id = Column(Integer, ForeignKey('riders.id'))\n    transfer_date = Column(String)  # Could be adjusted to a date type